from django.apps import AppConfig

class PayrollAccountingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "payroll_accounting"
    verbose_name = "Payroll Accounting"